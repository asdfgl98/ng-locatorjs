// Runtime
export {
  installAngularLocator,
  uninstallAngularLocator,
  getComponentMap,
  refreshComponentMap,
  type AngularLocatorOptions,
} from "./runtime";

// Server (for programmatic use)
export { startServer } from "./server";
